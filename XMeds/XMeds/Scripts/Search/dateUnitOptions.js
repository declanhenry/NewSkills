(function (ns) {

    ns.DateUnitOptions = function() {
        
        return {
            year: "year",
            month: "month",
            week: "week",
            day: "day"
        }
    };

}(window.Search = window.Search || {}));