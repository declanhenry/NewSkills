(function (ns) {

    ns.DateOperatorOptions = function () {

    };

    ns.DateOperatorOptions.prototype = function () {
        return {
            isOn: "is on",
            isNotOn: "is not on",
            isBefore: "is before"
        };
    }();

}(window.Search = window.Search || {}));