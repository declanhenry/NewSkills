(function (ns) {

    ns.parameterType = {
        numeric: "numeric",
        string: "string",
        choice: "choice",
        date: "date",
        time: "time",
    };

}(window.Search = window.Search || {}));