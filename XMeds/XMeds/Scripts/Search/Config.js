(function (ns) {

    ns.Config = function (description, typeOfConfig) {
        this.description = description;
        this.typeOfConfig = typeOfConfig;
    };

}(window.Search = window.Search || {}));